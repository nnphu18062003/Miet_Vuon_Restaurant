<div class="col-span-1 bg-white px-4 pb-6 shadow overflow-hidden rounded-lg mt-5">
  <div class="divide-y divide-gray-200 space-y-5">

    <!--region typeOfFood-->
    <div>
      <h2 class="text-xl text-gray-800 mb-3 uppercase font-medium mt-3">Bộ lọc tìm kiếm</h2>
      <h3 class="text-xl text-gray-800 mb-3 font-medium mt-3">Danh mục món ăn</h3>
      <form id="typeOfFood-filter-form">
        <div class="space-y-2">
          <% if (typeOfFoods && typeOfFoods.length > 0) { %>
            <% typeOfFoods.forEach((typeOfFood, index) => { %>
              <div class="flex items-center">
                <input 
                  type="checkbox" 
                  name="typeOfFoods" 
                  value="<%= typeOfFood %>" 
                  id="typeOfFood-<%= index %>" 
                  class="text-primary focus:ring-0 cursor-pointer"
                  <% if (selectedtypeOfFoods && selectedtypeOfFoods.includes(typeOfFood)) { %>
                    checked
                  <% } %> 
                   />
                <label for="typeOfFood-<%= index %>" class="text-gray-600 ml-3 cursor-pointer">
                  <%= typeOfFood %>
                </label>
              </div>
            <% }); %>
          <% } else { %>
            <p class="text-gray-500">No rands available for this section.</p>
          <% } %>
        </div>
      </form>
    </div>
    <!--region filter min max value-->
    <form id="price-filter-form" method="GET">
      <div class="pt-4">
        <h3 class="text-xl text-gray-800 mb-3 font-medium">Giá cả</h3>
        <div class="mt-4 flex items-center">
          <input type="text" name="min" id="min" class="rounded-md w-full border-gray-600 focus:border-primary focus:ring-0 px-3 py-1 text-gray-600 shadow-md" placeholder="min" value="" />
          <span class="mx-3 text-gray-500">-</span>
          <input type="text" name="max" id="max" class="rounded-md w-full border-gray-600 focus:border-primary focus:ring-0 px-3 py-1 text-gray-600 shadow-md" placeholder="max" value="" />
        </div>
      </div>
    </form>

    <div class="flex justify-center items-center">
      <button type="button" onclick="applyAndUpdateFilters()" class="mt-4 bg-[#66BB6A] text-white px-4 py-2 rounded-lg">Áp dụng</button>
    </div>
  </div>
</div>